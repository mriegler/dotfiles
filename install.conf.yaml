- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.spacemacs: spacemacs
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
